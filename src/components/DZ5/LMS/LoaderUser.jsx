import { ClockLoader } from "react-spinners";

const LoaderUser = () => {
  return (
    <div className="flex justify-center items-center">
      <ClockLoader color="white" size={400} />
    </div>
  );
};

export default LoaderUser;
